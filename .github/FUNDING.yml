# GitHub Sponsors — Support OpenAmer
# Funding platforms we actually accept.
# Re-verified 2026-09-14 by RENDERED PAGE, not by status code (a 200 is not proof):
#   LIVE  paypal hosted button (ncp/payment/3HMBFYC9CQTMS)
#   DEAD  issuehunt -> soft-404 (200 + "404" body) — an earlier "verified live
#         (200)" note here read the status code, not the page; removed.
#   DEAD  ko-fi (redirects to homepage), buymeacoffee (404),
#         github sponsors (302 to bare profile — never set up).
# A redirect to the platform homepage/profile == dead link; it costs trust.

# github: [openamer]  # Sponsors never set up (redirects to profile) — re-add after dashboard setup
patreon: # Replace with Patreon username
open_collective: # Replace with OpenCollective username
ko_fi: # dead — /openamer_agent redirects to ko-fi.com homepage; re-add when account exists
tidelift: # Replace with Tidelift platform
community_bridge: # Replace with Community Bridge project
liberapay: # Replace with Liberapay username
otechie: # Replace with Otechie username
custom: ['https://www.paypal.com/ncp/payment/3HMBFYC9CQTMS']