name: openai
version: 1.0.0
description: "OpenAI image generation backend (gpt-image-2). Saves generated images to $OPENAMER_HOME/cache/images/."
author: openamer
kind: backend
requires_env:
  - OPENAI_API_KEY
